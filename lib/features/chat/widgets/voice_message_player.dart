import 'package:flutter/material.dart';
class VoiceMessagePlayer extends StatelessWidget { const VoiceMessagePlayer({super.key}); @override Widget build(BuildContext context) => const Placeholder(); }

import 'package:flutter/material.dart';
class MediaPreviewWidget extends StatelessWidget { const MediaPreviewWidget({super.key}); @override Widget build(BuildContext context) => const Placeholder(); }
